package P_Utils is

   Type intArray is Array (Positive range <>) of Integer;
   Type posArray is Array (Positive range <>) of Positive;
   
   Type T_CompaComplete is (sup, inf, ega);
   
end P_Utils;
